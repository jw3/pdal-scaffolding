#pragma once

#include <pdal/Filter.hpp>

namespace pdal
{
    class PDAL_DLL @PLUGIN_NAME@ : public Filter
    {
    public:
        std::string getName() const;

    private:
        virtual void addArgs(ProgramArgs&) override;
        virtual void ready(PointTableRef) override;
        virtual PointViewSet run(PointViewPtr) override;
        virtual void done(PointTableRef) override;
    };
}
